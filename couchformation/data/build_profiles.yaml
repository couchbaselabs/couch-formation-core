---
generic:
  supports:
    - create
  parameters:
    - ssh_key:
        type: path
        allowed_values: []
        required_values: []
cbs:
  supports:
    - create
    - add
  parameters:
    - services:
        type: csv
        allowed_values:
          - data
          - index
          - query
          - fts
          - eventing
          - analytics
          - backup
          - manager-only
          - search
          - default
        required_values:
          - data
    - ssh_key:
        type: path
        allowed_values: []
        required_values: []
